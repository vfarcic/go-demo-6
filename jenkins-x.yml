buildPack: go
pipelineConfig:
  agent: {}
  pipelines:
    pullRequest:
      build:
        preSteps:
        - sh: make unittest
      promote:
        steps:
        - sh: echo 000
        - sh: echo `jx get preview --current` > ADDRESS
        - sh: echo 111
        - sh: echo `cat ADDRESS`
        - sh: echo 222
        - sh: echo ADDRESS=`cat ADDRESS` make functest
        - sh: echo 333
        - sh: ADDRESS=\$(jx get preview --current) make functest
