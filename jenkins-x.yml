buildPack: go
pipelineConfig:
  agent: {}
  pipelines:
    pullRequest:
      setup:
        preSteps:
          stages:
          - name: 111
            steps: echo '!!!!!!!!!!!!'
      build:
        preSteps:
        - sh: make unittest
      promote:
        steps:
        - sh: ADDRESS=`jx get preview --current 2>&1` make functest
