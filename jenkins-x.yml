buildPack: go
pipelineConfig:
  agent: {}
  pipelines:
    pullRequest:
      build:
        preSteps:
        - sh: make unittest
      promote:
        steps:
        - sh: env
        - sh: kubectl get pods
        - sh: ADDRESS=`jx get preview --current 2>&1` make functest
