buildPack: go
pipelineConfig:
  agent: {}
  pipelines:
    pullRequest:
      setup:
        preSteps:
        - command: echo
          loop:
            variable: ARCH
            values:
            - arch-1
            - arch-2
            - arch-3
            steps:
            - command: echo
              args:
              - this
              - is
              - a
              - loop
      build:
        preSteps:
        - sh: make unittest
      promote:
        steps:
        - sh: ADDRESS=`jx get preview --current 2>&1` make functest
