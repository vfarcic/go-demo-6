buildPack: go
pipelineConfig:
  agent: {}
  pipelines:
    pullRequest:
      build:
        preSteps:
        - sh: make unittest
      promote:
        steps:
        - sh: echo 000
        - sh: echo ADDRESS=`echo 123` make functest
        - sh: echo 111
        - sh: echo ADDRESS=`jx get preview --current | tee` make functest
        - sh: echo 222
        - sh: echo ADDRESS=\$(jx get preview --current) make functest
        - sh: echo 333
        - sh: ADDRESS=\$(jx get preview --current) make functest
