buildPack: go
pipelineConfig:
  agent: {}
  pipelines:
    pullRequest:
      setup:
        preSteps:
          - name: some-name
            parallel:
            - name: parallel-1
              sh: echo '!!!!!'
      build:
        preSteps:
        - sh: make unittest
      promote:
        steps:
        - sh: ADDRESS=`jx get preview --current 2>&1` make functest
