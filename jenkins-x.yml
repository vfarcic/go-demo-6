buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: go
        stages:
        - name: ci
          steps:
          - command: echo "This is a silly pipeline"
            name: silly
